# Supabase connection for the live shared leaderboard + lead capture.
# Copy this file to .env and fill both values to go live.
#
# Leave BOTH blank to run the local demo backend (localStorage, offline,
# seeded leaderboard, no leads stored). The app switches automatically.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
